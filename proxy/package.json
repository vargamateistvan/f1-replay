{
  "name": "f1-replay-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that proxies OpenF1 API requests and caches responses in KV.",
  "packageManager": "yarn@1.22.22",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260808.1",
    "typescript": "^5.6.3",
    "wrangler": "^3.101.0"
  }
}
